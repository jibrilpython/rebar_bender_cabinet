include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # TmpleteApp is a reference template — not part of the actual project
    - TmpleteApp/**

linter:
  rules:
    # avoid_print: false
    # prefer_single_quotes: true
